export const IMAGE_CONFIG = {
  dataBits: 8,        // Number of bits to extract/store
  scale: 10,          // Export scale factor
  color: '#ec4899',   // Foreground color (text-pink-500)
  threshold: 125      // Single threshold for all channels
} as const;
